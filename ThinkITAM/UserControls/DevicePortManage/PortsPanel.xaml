<UserControl
    x:Class="ThinkITAM.UserControls.DevicePortManage.PortsPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:local="clr-namespace:ThinkITAM.UserControls.DevicePortManage"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:Background="AliceBlue"
    d:Height="768"
    d:Width="1366"
    FontFamily="{StaticResource HarmonyOsFontFamily}"
    mc:Ignorable="d">
    <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect SlideInFromBottom, Duration=0:0:0.5}">

        <materialDesign:ColorZone
            Margin="5"
            d:DesignHeight="300"
            d:DesignWidth="600"
            BorderThickness="1"
            CornerRadius="5">

            <Grid Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        Margin="10,10"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        d:Text="ABCDEFG"
                        FontSize="12"
                        Opacity="0.8"
                        Text="{Binding SlotNumber}"
                        TextWrapping="NoWrap" />

                    <Button
                        x:Name="RemoveSlotButton"
                        Grid.Column="1"
                        HorizontalAlignment="Right"
                        Click="RemoveSlotButton_OnClick"
                        Style="{StaticResource MaterialDesignIconButton}"
                        ToolTip="{DynamicResource DpmDeleteSlot}">
                        <materialDesign:PackIcon
                            Width="24"
                            Height="24"
                            Kind="ServerRemove" />
                    </Button>
                </Grid>



                <ItemsControl
                    Grid.Row="1"
                    dd:DragDrop.IsDragSource="True"
                    dd:DragDrop.IsDropTarget="True"
                    ItemsSource="{Binding Ports}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Left" Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:DevicePort Margin="5,0" DataContext="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>


            </Grid>

        </materialDesign:ColorZone>
    </materialDesign:TransitioningContent>
</UserControl>
